# MIDNIGHT REPO INTELLIGENT --- EXECUTION 13

## Performance-Native Fusion & Learning-Pressure Engine

**Parent:** Midnight Performance **Purpose:** Make Performance the
temporal nervous system of Repo Intelligent rather than one optional
data source.

## Goal

Implement one canonical fusion path that transforms qualified
Performance evidence into project-learning pressure, knowledge-gap
questions, and later learning-outcome evaluation.

## Parent loop

`BASELINE → PERFORMANCE CONTRACT MAP → SIGNAL MODEL → FUSION PLAN → FALSIFY PLAN → IMPLEMENT → VERIFY → REVIEW → REPAIR → FINAL GATE`

## Required work

1.  Inspect real Performance resources/projections and map every Repo
    Intelligent need to an existing canonical owner before adding state.
2.  Implement `PerformanceLineageReceipt`.
3.  Implement `ProjectLearningPressure` with attention, friction,
    recurrence, uncertainty, impact, knowledge deficit and freshness.
4.  Implement `PerformanceQuestionCompiler`.
5.  Reuse anomaly semantics: unusual ≠ bad.
6.  Reuse personal-learning history matching instead of inventing a
    second user-history model.
7.  Reuse recommendation/outcome semantics for later associative
    evaluation.
8.  Reuse AI accounting for model cost and extend accounting to
    search/fetch only through a compatible typed ledger.
9.  Make missing Performance/Memory context an explicit gap.
10. Add query surfaces such as:

-   active learning pressures;
-   why this topic now;
-   evidence behind pressure;
-   internal knowledge sufficiency;
-   research jobs spawned from a pressure;
-   exposures and later outcome associations.

## Critical invariant

A proactive external research job cannot exist without a valid
project-scoped Performance lineage receipt, except when the user
explicitly asks for external research.

## Verification scenarios

-   active healthy refactor;
-   chronic repeated failure;
-   unusual but successful one-off task;
-   recurring component with old Memory answer;
-   recurring component whose prior answer is stale;
-   no Performance installed/degraded bridge;
-   cross-project attack;
-   rerun/restart idempotency;
-   cost accounting reconciliation.

Final report: `GOAL YES/PARTIAL/NO`, evidence, files, tests, gaps.
